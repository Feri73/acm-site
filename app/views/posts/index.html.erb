<div class="content">
  <div class="blog-name"><%= Blog.last.name %></div>
  <div class="blog-desc"><%= Blog.last.desc %></div>
  <div class="post-wrp">
    <div class="side-line">
    </div>
    <% @posts.order(:publishDate).each do |post| %>
        <%= render 'posts/post', post: post, summary: true %>
    <% end %>
  </div>
</div>
