<% if @error %>
alert("<%=@error%>");
<% else %>
$("#comment-<%=@commen_id%>").remove();
new PNotify({
    text: 'نظر شما حذف شد!',
    type: 'warning',
    styling: 'fontawesome',
    icon: false
    <% if I18n.locale.to_s == 'fa' %>, addclass: 'text-fa'
    <%end %>
});
<% end %>